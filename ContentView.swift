import SwiftUI

struct ContentView: View {
    @State var Number1 = 0
    @State var integer1: Int = 0
    @State var integer2: Int = 0
    var body: some View {
        Button {
            
        } label: {
            Text("Multiply")
        }

    }
}
